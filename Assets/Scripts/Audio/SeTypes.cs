using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// SEのタイプ
/// </summary>
public enum SeType
{
    seSelect,
    seDecide,

    seClick,
    seSlide,
    seGetScore,

    seGameReady,
    seGameStart,
    seGameOver,
}

